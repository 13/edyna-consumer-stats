services:
  edyna-scraper:
    build: .
    container_name: edyna-scraper
    restart: unless-stopped
    env_file:
      - .env
    environment:
      # Force headless in containers regardless of .env
      - HEADLESS=true
