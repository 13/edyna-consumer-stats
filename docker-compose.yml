services:
  edyna-scraper:
    build: .
    container_name: edyna-scraper
    restart: unless-stopped
    env_file:
      - .env
    environment:
      # Force headless in containers regardless of .env
      # (EDYNA_-prefixed form wins over unprefixed, see src/config.js)
      - EDYNA_HEADLESS=true
